# Day62：导入导出（mysqldump 与恢复）

今天你只要按顺序完成一次 **导出 → 删除 → 恢复 → 验证**。

## 1) 打开并照做

直接按文档执行：

- `week9_backup_restore.md`

## 2) 验收（做完打钩）

- [ ] 成功导出 `learn_java`（全量或结构+数据）
- [ ] 删除一个测试表（例如 `account`）
- [ ] 从备份恢复
- [ ] 用 `SHOW TABLES` 和 `SELECT` 验证恢复成功

