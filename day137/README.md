# Day137：Linux-1（目录、权限、进程与端口）

## 交付

- **`deploy/linux-notes.md`**：可复制命令（**`ls` / `chmod` / `chown`**、**`ss` / `ps`**、**`journalctl`**、本仓库 **Tomcat `logs/`**）。

## 验收

- 能独立说明：**应用起不来**时优先看 **端口 → 日志 → 权限**；并能在本机用 **`ss`**、**`tail -f catalina.out`** 各完成一次实操。
