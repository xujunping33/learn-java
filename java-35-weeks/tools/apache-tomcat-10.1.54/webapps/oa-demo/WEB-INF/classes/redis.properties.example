# 复制为 redis.properties 后按需修改（redis.properties 建议不入库，见 README）
#
# Day140：未配置或 redis.enabled=false 时，不连 Redis，GET /api/leaves/me 直接查库（与 Day139 前行为一致）。
redis.enabled=false
redis.host=127.0.0.1
redis.port=6379
redis.password=
# GET /api/leaves/me 旁路缓存 TTL（秒），建议 30–60
redis.leavesMeTtlSeconds=45
