# Day 115：Web 标准结构与 war 布局

- 工程：**`../servlet-demo/`**（已含 **`src/main/webapp`**、**`WEB-INF/web.xml`**、欢迎页、**`WEB-INF/lib`** 依赖示例）。
- 自查 war：**`jar tf target/servlet-demo.war | grep WEB-INF`**（见 **`../servlet-demo/README.md`**）。
- **`WEB-INF` 为何不能直链访问**：**`../W17-notes.md` → Day 115**。
