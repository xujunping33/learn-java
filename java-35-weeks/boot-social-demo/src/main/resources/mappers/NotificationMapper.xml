<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="learn.java.bootsocial.mapper.NotificationMapper">

    <insert id="insertIgnore" parameterType="learn.java.bootsocial.model.Notification">
        INSERT IGNORE INTO notifications (user_id, type, ref_id, payload, dedup_key)
        VALUES (#{userId}, #{type}, #{refId}, #{payload}, #{dedupKey})
    </insert>

</mapper>

